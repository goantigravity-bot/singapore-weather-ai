# 待处理问题 — 2026-02-08

> 来自 2026-02-07 训练流水线修复后的遗留问题

## 问题列表

### 1. 邮件通知中 epoch 数显示不正确
- **现象**: 邮件中显示的 epoch 数不是实际训练的 epoch 数
- **可能原因**: `training_metrics.json` 中 `final_epoch` 字段名不匹配，或 metrics 未正确传递到邮件模板
- **涉及文件**: `notification.py`, `training_scheduler.py`, `train.py`

### 2. 仪表盘训练时长显示 "N/A"
- **现象**: 监控仪表盘的训练历史中，时长列显示 "N/A"
- **可能原因**: `training_history.json` 中缺少训练耗时字段，或前端读取字段名不匹配
- **涉及文件**: `monitor_api.py`, 前端仪表盘代码

### 3. 下载服务器 NEA 数据显示 0/4
- **现象**: 监控仪表盘的文件下载服务器 tab 中，NEA 数据显示 0/4
- **可能原因**: `monitor_api.py` 中检查 NEA 数据的逻辑与实际存储路径或文件命名不一致
- **涉及文件**: `monitor_api.py`, 下载服务器脚本

### 4. 前端应用地点显示不正确
- **现象**: Web 应用前端的地点信息显示不正确
- **可能原因**: 地理编码或地点数据源问题
- **涉及文件**: 前端组件代码

### 5. 前端应用界面语言切换为英文
- **现象**: 当前应用界面显示为中文
- **需求**: 将前端显示语言改为英文
- **涉及文件**: 前端组件代码、文案/i18n 相关文件
